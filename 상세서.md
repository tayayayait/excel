---
title: 업무기록 기반 자동 요약·시각화 시스템 UI/UX 상세서 (Excel MVP)
version: v0.2
last_updated: 2026-01-22
audience: 기획/디자인/개발(엑셀 구현) 공통
---

# 0. 문서 목적/범위
본 문서는 “직원이 최소 입력(오늘 한 일, 소요시간)”만 하면 **월간·프로젝트·일간·주간 상태가 자동 요약/시각화**되는 Excel 기반 업무 관리 시스템의 **UI/UX 구현 기준**을 정의합니다.  
엑셀 MVP에서 바로 구현 가능한 규칙(레이아웃, 색상 팔레트, 컴포넌트 규격, 상호작용, 오류/빈데이터 처리)을 **명세 수준**으로 제공합니다.  
추후 웹으로 확장 시에도 동일한 디자인 토큰(색/타이포/컴포넌트 규칙)을 재사용할 수 있도록 작성합니다.

> 핵심 철학: “얼마나 했는가”가 아니라 “올바른 방향으로 가고 있는가”를 **기록 데이터**로 확인한다.

---

# 1. UX 원칙(우선순위)
1) **입력 최소화**: 직원은 입력 시트 1개만 사용(기본 2필드). 다른 시트는 열람 전용.  
2) **판단 자동화**: 체크/주간 평가 없음. 기록이 있으면 완료로 간주(반복업무), 흐름은 활동/정체로 표시(프로젝트).  
3) **이해는 시각화**: 숫자표 + 추세(스파크라인) + 상태색(신호등)로 즉시 파악.  
4) **오류 예방**: 유효성 검증/예시/자동서식으로 “잘못 쓰기 어렵게”.  
5) **일관성**: 모든 대시보드는 동일한 상단 필터/카드 규격/색 의미를 공유.  
6) **확장성**: 엑셀 UI 규칙을 웹 컴포넌트로 1:1 매핑 가능하도록 토큰화.

---

# 2. 정보구조(Workbook IA) & 내비게이션
## 2.1 시트 구성(권장)
- `00_HOME` : 홈/설명/바로가기(읽기 전용)
- `10_INPUT` : 직원 입력 전용(유일한 입력 접점)
- `20_DAILY` : 일간 요약(오늘/선택일)
- `30_WEEKLY` : 주간 평균·비교(이번주 vs 지난주 등)
- `40_MONTHLY` : 월간 반복업무 이행/분포
- `50_PROJECTS` : 프로젝트 흐름 + 간트(활동/정체)
- `90_SETTINGS` : 관리자 설정(프로젝트/반복업무/태그 규칙/임계치)
- `99_DATA` : 원천/가공 데이터(숨김 권장, 읽기 전용)

## 2.2 시트 탭(Worksheet Tab) 색 규칙
- 입력(직원): Green 계열(“여기만 쓰면 된다”)
- 대시보드: Blue 계열(열람/분석)
- 설정/데이터: Gray 계열(관리자 영역)

## 2.3 내비게이션 규칙
- 모든 대시보드 상단 좌측에 **고정 내비게이션 바(홈, 입력, 일간, 주간, 월간, 프로젝트)** 버튼 배치
  - 구현: 도형(사각형) + 하이퍼링크(해당 시트 A1로 이동)
- `00_HOME`는 “사용 안내 + 빠른 이동”을 포함(초보자 진입점).
- 입력 시트(`10_INPUT`)는 **가장 단순하게**: 메뉴 최소/시각 장식 최소(입력 방해 요소 제거).

---

# 3. 공통 레이아웃 규칙(Excel 구현 기준)
## 3.1 캔버스(가시 영역) 규격
- 기준 해상도: 노트북 14~16"에서 100% 확대(Windows 기본)
- 대시보드 주요 콘텐츠는 **A~AI 열(약 35~40열)** 안에 들어오게 설계(가로 스크롤 최소화)
- 각 시트 상단 1~6행은 공통 헤더 영역으로 사용(내비게이션/타이틀/필터/요약)

## 3.2 공통 헤더 템플릿(행 기준)
모든 대시보드(`20~50_*`)는 아래 헤더 행 역할을 동일하게 유지합니다.
- 1행: 패딩(빈 행 또는 얇은 라인)
- 2행: 내비게이션 버튼 영역
- 3행: 화면 타이틀(H1) + 설명 1줄(Caption)
- 4~5행: 필터 바(기간/팀원/프로젝트/초기화)
- 6행: 상단 요약(소형 KPI 또는 상태 메시지)

## 3.3 틀 고정(Freeze Pane)
- 대시보드: **6행까지 고정**(7행부터 스크롤)
- 입력: **입력 폼이 사라지지 않도록 8~10행까지 고정**

## 3.4 표준 행 높이/열 너비(권장값)
> 실제 화면/폰트에 따라 미세 조정 가능. “일관성”이 최우선이며, 시트별로 제각각이면 안 됩니다.

### 행 높이(Points)
- 패딩 행: 6~10pt
- 헤더 행(2~6): 20~26pt
- 본문 기본: 18pt
- 카드 제목: 20pt
- KPI 숫자 포함 카드: 24~32pt(숫자가 잘리지 않도록)

### 열 너비(Characters)
- 패딩 열(A): 2
- 구터(카드 사이 빈 열): 2~3
- 라벨 열: 10~14
- KPI/숫자 열: 8~10
- 긴 텍스트 열(업무/프로젝트명): 24~40(줄바꿈 허용)

## 3.5 보호(Protect Sheet) 원칙(권장)
- 대시보드/설정: 셀 잠금(편집 방지), **필터/슬라이서 조작은 허용**
- 입력: 입력 셀/테이블 입력 열만 편집 허용(나머지 잠금)

## 3.6 그리드라인/배경(시각 노이즈 최소화)
- 대시보드(`20~50_*`): 눈금선(Gridlines) 숨김 + 시트 배경 `Slate-50`로 통일
- 입력(`10_INPUT`): 눈금선은 선택(입력 정확도가 우선이면 표시), 배경은 White 유지

## 3.7 레이아웃 좌표 표기(추천 기준)
본 상세서의 “좌표”는 Excel A1 기준 표기를 사용합니다.  
권장 레이아웃은 **콘텐츠 시작 열을 B로 고정(열 A는 패딩)**하며, 모든 대시보드에 동일하게 적용합니다.

- 공통 헤더 영역(대시보드): `B1:AI6`
- 공통 본문 시작 행: 8행(`B8`부터)
- 좌/우 2단 분할(권장): 좌측 `B:Q`, 우측 `R:AI`

--- 

# 4. 컬러 시스템(토큰) — Excel/웹 공통
## 4.1 기본 팔레트(HEX / RGB)
엑셀 테마 색상(Accent)으로 등록해 재사용합니다.

| Token | 용도 | HEX | RGB |
|---|---|---:|---:|
| `Primary-600` | 기본 강조/링크/프로젝트 | `#2563EB` | 37, 99, 235 |
| `Teal-600` | 반복업무/지속성 | `#0D9488` | 13, 148, 136 |
| `Amber-500` | 주의/정체 경고 | `#F59E0B` | 245, 158, 11 |
| `Red-500` | 오류/위험 | `#EF4444` | 239, 68, 68 |
| `Violet-500` | 학습/개선/실험(선택) | `#8B5CF6` | 139, 92, 246 |
| `Slate-900` | 본문 텍스트 | `#0F172A` | 15, 23, 42 |
| `Slate-600` | 보조 텍스트 | `#475569` | 71, 85, 105 |
| `Slate-200` | 보더/구분선 | `#E2E8F0` | 226, 232, 240 |
| `Slate-50` | 배경 | `#F8FAFC` | 248, 250, 252 |
| `White` | 카드 배경 | `#FFFFFF` | 255, 255, 255 |

## 4.2 의미(semantic) 규칙(고정)
- **프로젝트**: `Primary-600` 계열
- **반복업무**: `Teal-600` 계열
- **정체/주의**: `Amber-500`
- **오류/위험**: `Red-500`
- 중립 요소(표 그리드/축/카드 보더): `Slate-200`
- 텍스트: `Slate-900`(본문) / `Slate-600`(보조)

## 4.3 상태 표시(색 + 텍스트 동시)
색만으로 의미를 전달하지 않습니다. 모든 상태는 **뱃지 텍스트**를 반드시 포함합니다.
- `활동`: 파랑 + “활동”
- `정체`: 주황 + “정체”
- `데이터 부족`: 회색/주황 + “데이터 부족”
- `오류`: 빨강 + “오류”

## 4.4 조건부서식(틴트) 규칙
- 의미색은 “진한 원색”을 직접 칠하지 않고, **연한 틴트(10~20%)**로 배경을 칠합니다.
- 진한 색은 보더/아이콘/텍스트/막대(차트)에서만 사용합니다.

---

# 5. 타이포그래피(Excel 구현 기준)
## 5.1 폰트
- 기본: `맑은 고딕`(Windows), 대체: `Segoe UI`

## 5.2 텍스트 스타일(셀 스타일로 등록 권장)
| Style | 용도 | 크기(pt) | 굵기 | 색 |
|---|---|---:|---|---|
| `H1` | 시트 타이틀 | 18 | Bold | Slate-900 |
| `H2` | 섹션 타이틀 | 13 | Bold | Slate-900 |
| `Label` | 필터 라벨/필드명 | 10 | Semibold | Slate-600 |
| `Body` | 일반 텍스트 | 10.5 | Regular | Slate-900 |
| `Metric` | KPI 숫자 | 20~28 | Bold | Slate-900 |
| `Caption` | 보조 설명/주석 | 9 | Regular | Slate-600 |

## 5.3 숫자/시간 표기(일관 규칙)
- 시간 입력 단위: **분(min)** (정수)
- 대시보드 표기:
  - 합계/평균은 `h:mm`로 표기(예: 450분 → `7:30`)
  - 필요 시 괄호로 분 병기(예: `1:15 (75m)`)
- 소수점 금지(해석 비용 증가). 평균 분은 5분 단위 반올림(권장).

---

# 6. 컴포넌트 스펙(규격/상태/사용 규칙)
> 엑셀에서는 “픽셀”보다 “셀 규격”이 중요하므로, 기본 규격은 **셀 기반(표/카드)**으로 정의합니다.  
> 도형은 버튼/탭 등 최소 범위로 사용(유지보수/정렬 부담 감소).

## 6.1 버튼(Button)
### 사용처
- 시트 이동, 필터 초기화, (선택) “오늘 기록 추가”

### 규격(권장)
- 높이: 20~24pt(1행)
- 가로: 3~6열 폭(버튼 텍스트가 한 줄로 유지되도록)
- `Primary`: 배경 `Primary-600`, 글자 White
- `Ghost`: 배경 White, 보더 `Slate-200`, 글자 `Slate-900`
- `Danger`: 배경 `Red-500`, 글자 White

### 상태
- Hover 없음(Excel). 대신 “선택/활성”은 보더 2pt 또는 음영 10% 진하게.

## 6.2 입력창(Input Field) — 직원 입력용
### 구성(권장: 상단 폼 + 로그 테이블)
- 상단 “오늘 입력 폼”: `업무(텍스트)` + `분(숫자)` + `추가` 버튼
- 하단 “누적 로그 표”: 날짜/업무/분(직원은 여기 직접 수정 가능/불가 선택)

### 입력 도움말(고정 문구)
사용자가 규칙을 “외우지 않게” 오른쪽에 예시 2~3개를 항상 표시합니다.

## 6.3 카드(Card)
### 공통 규격(셀 기반)
- 배경: White
- 보더: `Slate-200` 1pt
- 내부 여백: 상/하 1행, 좌/우 1열(패딩 열/행으로 확보)

### 카드 사이즈(권장)
| Size | 용도 | 가로(열) | 세로(행) |
|---|---|---:|---:|
| `Card-S` | 소형 KPI | 6~8 | 4~5 |
| `Card-M` | 기본 KPI/리스트 | 10~14 | 6~8 |
| `Card-L` | 차트 컨테이너 | 16~24 | 10~16 |

### 카드 타입
- `KPI Card`: 타이틀 + 큰 숫자(`Metric`) + 전주대비(▲▼) + 스파크라인(7~14일)
- `Status Card`: 상태 뱃지 + 근거 문장 1줄(“최근 7일 기록 없음”)
- `List Card`: Top5 항목(프로젝트/업무) + 합계 시간

## 6.4 뱃지/칩(Badge/Chip)
### 규칙
- 높이: 1행 안에 들어오게(14~18pt)
- 배경: 의미색 10~15% 틴트, 텍스트: 의미색 진한 톤
- 동일 의미는 항상 동일 색/단어 사용(예: 정체=Amber+“정체”)

## 6.5 테이블(Table)
### 규칙
- Excel “표(Format as Table)” 사용(필터 포함)
- 헤더: `Slate-50` 배경 + Bold + 하단 보더 `Slate-200`
- 행 배경: 연한 지그재그(가독성)
- 정렬: 숫자 오른쪽, 텍스트 왼쪽, 날짜 중앙
- 텍스트가 긴 컬럼은 줄바꿈 허용(업무/설명), KPI 표는 줄바꿈 금지

## 6.6 차트(Chart) 컨테이너
### 공통 규칙
- 차트는 반드시 “카드(L)” 안에 배치(화이트 배경 + 보더)
- 축/그리드라인은 `Slate-200`로 얇게(정보를 가리지 않게)
- 범례는 오른쪽 위 또는 아래(차트 영역을 침범하지 않게)

## 6.7 모달(Modal) / 설정 UI(대안 포함)
엑셀 MVP에서 모달은 유지보수 부담이 있으므로 2단계 옵션을 정의합니다.

1) **권장(무매크로)**: `90_SETTINGS` 시트를 “사이드패널”처럼 구성  
   - 좌측: 목록/표, 우측: 선택 항목 상세/편집 폼
2) **선택(VBA/Office Scripts)**: UserForm(모달) 제공  
   - 폭 480~560px, 2열 폼(라벨/입력), 하단 버튼(저장/취소)

---

# 7. 입력 UX(직원) — 최소 입력을 지키는 규칙
직원이 2필드만 입력해도 분류/집계가 가능하도록, 업무 텍스트에 “접두어 규칙”을 적용합니다.

## 7.1 업무 텍스트 최소 규격(자동 분류용)
- 프로젝트: `P:프로젝트명 | 작업`  
  - 예: `P:쇼핑몰리뉴얼 | 결제 모듈 오류 재현`
- 반복업무: `R:업무명 | 작업`  
  - 예: `R:월간정산 | 1월 전표 검수`
- 기타/지원(선택): `S:업무명 | 작업`
- 긴급(선택): 끝에 `#긴급` 태그

## 7.2 유효성 검증(입력 오류 방지)
- `분`은 정수(1~720 권장). 범위 밖은 Amber(확인) 또는 Red(오류).
- `업무`가 비어있으면 저장/추가 불가(스크립트 사용 시) 또는 행 경고 표시.

---

# 8. 시트별 UI/UX 상세 설계

## 8.1 `00_HOME` (홈)
### 목적
처음 사용자(직원/리더)가 “어디서 입력하고 무엇을 볼지” 즉시 이해.

### 구성(권장)
- 상단: 시스템명(H1) + 목적 1문장(Caption)
- 중앙: 큰 바로가기 카드 5개(입력/일간/주간/월간/프로젝트)
- 하단: 입력 규칙 요약(접두어 예시) + “데이터가 없을 때 화면은 이렇게 보입니다” 안내

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI6` | 타이틀/설명/내비게이션 |
| 바로가기 카드 | `B8:AI22` | 5개 카드(입력/일간/주간/월간/프로젝트) |
| 사용 가이드 | `B24:AI40` | 입력 규칙/빈 데이터 안내 |

## 8.2 `10_INPUT` (직원 입력 시트) — 핵심
### 목적
직원이 **최소 입력**으로 기록을 남기고, 기록이 데이터로 축적되게 함.

### 화면 구조(상단→하단)
1) 오늘 입력 폼(상단 고정)  
2) (선택) 오늘 요약(소형 KPI 3~4개)  
3) 누적 기록 테이블(로그)  

### 누적 기록 테이블(필수 컬럼)
- 표시/입력: `일자`, `업무`, `분`
- 숨김(가공): `구분(P/R/S)`, `프로젝트명`, `태그`, `주차`, `월`

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 타이틀/안내 | `B1:AI3` | “입력만 하면 자동 요약” 1줄 + 최소 규칙 |
| 오늘 입력 폼 | `B4:AI10` | 업무/분 + 예시 + (선택) 추가 버튼 |
| 오늘 요약(선택) | `B11:AI15` | 소형 KPI 3~4개 |
| 누적 로그 표 | `B17:AI120` | 일자/업무/분(표 형식) |

## 8.3 `20_DAILY` (일간 요약)
### 목적
선택한 날짜(기본: 오늘)의 “방향성”을 한 화면에 요약.

### 구성(권장)
- KPI 카드 4개: 총 시간, 프로젝트 시간, 반복업무 시간, 기록 건수
- “오늘 한 일” 리스트(업무 Top N + 시간)
- 시간 분포 차트(구분/프로젝트별)
- 리스크 신호 카드: 반복업무 누락/기록 공백/급격한 편차 등

### 빈 데이터 처리
- 해당 날짜 기록이 없으면 “기록 없음” 상태 카드(Amber) + 차트는 빈 상태 메시지로 대체

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI6` | 타이틀 + 필터 바(날짜/팀원/프로젝트) |
| KPI(4개) | `B8:AI13` | 총/프로젝트/반복/건수 |
| 오늘 한 일(리스트) | `B15:Q40` | 업무 Top N + 시간 |
| 시간 분포(차트) | `R15:AI40` | 구분/프로젝트별 |
| 리스크 신호 | `B42:AI55` | 누락/공백/편차 |

## 8.4 `30_WEEKLY` (주간 평균·비교)
### 목적
이번주 vs 지난주(또는 4주 평균)로 “바쁘기만 한지/방향이 맞는지”를 비교.

### 구성(권장)
- KPI 카드 6개: 이번주 총시간, 지난주 총시간, Δ, 프로젝트 비중, 반복 비중, 기록일수
- 비교 차트: 일별 총시간 라인(2주 겹침), 구분별 누적 막대(이번주/지난주)
- Top 변화: 증가/감소 프로젝트 Top3

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI6` | 타이틀 + 주/팀원 필터 |
| KPI(6개) | `B8:AI14` | 2행 배치 권장 |
| 비교 차트(2개) | `B16:AI40` | 좌(라인) `B:Q` / 우(막대) `R:AI` |
| Top 변화 | `B42:AI60` | 증가/감소 Top3 |

## 8.5 `40_MONTHLY` (월간 반복업무 + 월간 분포)
### 목적
반복업무를 “체크리스트”가 아니라 **기록 기반 자동 완료**로 보이게 함.

### 구성(권장)
- 반복업무 이행률 KPI + “미기록 반복업무” 리스트
- 캘린더 히트맵(일자별 기록 유무/강도)
- 월간 시간 분포(프로젝트/구분)

### 반복업무 이행 UI 규칙
- `완료`: 해당 월에 해당 반복업무(`R:`) 기록이 1건 이상
- `미완료`: 0건
- `주의`: 월의 70%가 지났는데 미완료(Amber)

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI6` | 타이틀 + 월 필터 |
| 반복 KPI/미기록 | `B8:Q26` | 이행률 + 미기록 리스트 |
| 캘린더 히트맵 | `R8:AI26` | 일자별 기록 강도 |
| 월간 분포 | `B28:AI55` | 프로젝트/구분 분포 |

## 8.6 `50_PROJECTS` (프로젝트 흐름 + 간트)
### 목적
프로젝트를 “보고서”가 아니라 **활동 흐름(진행/정체/재개)**으로 보여줌.

### 구성(권장)
- 프로젝트 상태 카드 그리드
  - 프로젝트명 / 최근 기록일 / 최근 N일 투입시간 / 상태 뱃지(활동/정체)
- 활동 구간 간트(일 단위)
  - 기록이 있는 날짜 셀을 파랑 틴트로 채움(연속이면 막대처럼 보이게)
  - 공백이 길면(예: 7일+) Amber 틴트로 “정체 구간”을 배경 강조

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI6` | 타이틀 + 월/프로젝트 필터 |
| 프로젝트 카드 보드 | `B8:AI26` | 프로젝트 상태 카드 그리드 |
| 활동 구간 간트 | `B28:AI60` | 일 단위 활동 표시 |
| 최근 기록 목록(선택) | `B62:AI90` | 선택 프로젝트 상세 로그 |

## 8.7 `90_SETTINGS` (관리자 설정)
### 목적
자동판단의 기준(태그 규칙, 반복업무 목록, 프로젝트 목록, 임계치)을 UI에서 관리.

### 권장 테이블
- 프로젝트 목록(프로젝트명, 활성/비활성, 담당(선택))
- 반복업무 목록(반복명, 주기, 적용월)
- 태그 규칙(P/R/S 접두어, 긴급 태그 등)
- 임계치(정체 기준 N일, 이상치 분 기준 등)

### 레이아웃 좌표(추천)
| 영역 | 좌표(대략) | 내용 |
|---|---|---|
| 헤더 | `B1:AI5` | “설정(관리자)” + 주의 문구 |
| 좌측(목록/테이블) | `B6:Q80` | 프로젝트/반복업무/태그 테이블 |
| 우측(상세/편집) | `R6:AI80` | 선택 항목 편집 폼(사이드패널) |
| 임계치/룰 | `B82:AI120` | 정체/이상치 기준, 분류 규칙 |

--- 

# 9. 상호작용 규칙(필터/드릴다운/리셋)
## 9.1 필터 바(공통)
- 위치: 모든 대시보드 상단 우측(4~5행)
- 구성 순서: 기간(월/주/일) → 팀원 → 프로젝트 → 초기화
- 구현 우선순위:
  1) 피벗 + 슬라이서/타임라인(권장)
  2) 데이터 유효성 드롭다운 + 수식 기반 필터(대안)

## 9.2 초기화(Reset)
- 버튼 1개로 “전체 보기”로 복귀
- 초기화 후 상단에 “전체(ALL)” 텍스트를 표시(필터 상태가 보이게)

## 9.3 드릴다운(상세 보기)
- 카드 클릭 대신, 카드 우측 상단에 `상세 보기` 텍스트 링크(하이퍼링크)로 통일

---

# 10. 오류/빈 데이터/이상치 UI 처리 규칙
## 10.1 입력 오류(직원)
- `분`이 숫자가 아니면: 셀 배경 Red 틴트 + “숫자(분)만 입력”
- `분`이 720 초과(권장 임계치): Amber 틴트 + “장시간(입력 확인)”
- `업무`가 비어있으면: 저장/추가 불가(스크립트 사용 시) 또는 경고 표시

## 10.2 빈 데이터(대시보드)
- 기간 내 기록이 부족하면(예: 주간 기록일수 2일 이하):
  - KPI는 값 대신 “데이터 부족”
  - 차트는 빈 상태 메시지로 대체(축/범례만 남지 않게)

## 10.3 이상치(해석 왜곡 방지)
- 특정 하루 과도한 분 기록 시:
  - 차트가 찌그러지지 않도록 표시용 상한을 두거나(클램프), 이상치 마커를 분리
  - “이상치 포함” 배지로 투명하게 알림

---

# 11. 웹 전환을 위한 매핑 규칙(토큰 재사용)
## 11.1 토큰 → CSS 변수(예)
- `Primary-600` → `--color-primary-600`
- `Slate-50/200/600/900` → 배경/보더/텍스트

## 11.2 반응형 레이아웃(웹 기준 초안)
- Desktop(≥1200): 3~4열 카드 그리드
- Tablet(≥768): 2열 카드
- Mobile(<768): 1열 스택 + 상단 필터는 접기

---

# 12. 구현 완료 기준(체크리스트)
## 12.1 일관성
- 모든 대시보드에 공통 내비게이션/필터 바가 존재한다.
- 색 의미(프로젝트=파랑, 반복=청록, 경고=주황, 오류=빨강)가 전 시트에서 동일하다.

## 12.2 입력 경험
- 직원은 `10_INPUT`에서 2필드(업무/분) 중심으로 기록을 남길 수 있다.
- 입력 오류가 발생하면 즉시 시각적으로 알 수 있고(색/문구), 수정 안내가 명확하다.

## 12.3 요약/시각화
- 일간/주간/월간/프로젝트 화면이 데이터가 없을 때도 빈 상태 UI를 제공한다.
- 프로젝트 화면에서 활동/정체 상태가 한눈에 구분된다.

## 12.4 유지보수
- 색/폰트/카드/표 스타일이 “셀 스타일/테마” 중심으로 구성되어 수정이 쉽다.
- 도형 사용은 버튼/탭 등 최소 범위로 제한되어 정렬 이슈가 과도하지 않다.
